name: ghcdoc

dependencies:
        - base
        - binary
        - bytestring
        - Cabal
        - cmdargs
        - containers
        - edit-distance
        - filepath
        - Glob
        - lens
        - process
        - process-extras
        - split
        - text
        - unix
        - zstd
        - directory
        - happstack-server
        - blaze-html
        - tagsoup
        - HTTP
        - network-uri

executables:
        ghcdoc:
                main: main.hs
                other-modules:
                        HttpClient
